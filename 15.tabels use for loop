#include<stdio.h>
int main(){
    int a=0,num;
    printf("enter the number\n");
    scanf("%d",&num);
    for(a=1;a<=10;a++)
    {
        printf("%dx %d = %d\n",num,a,a*num);
    }
    

    return 0;
}